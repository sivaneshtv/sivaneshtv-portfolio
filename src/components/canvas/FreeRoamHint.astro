---
---
<div class="roam-hint">Locked view — unlock to free roam</div>

<style>
  /* §6.6 */
  .roam-hint{
    position:fixed;
    /* left/bottom overridden by JS to anchor above reader button */
    bottom:80px;left:50%;
    transform:translateX(-50%);
    z-index:35;
    background:rgba(26,26,28,.92);color:#f5ede0;
    padding:6px 12px;
    font-family:var(--fm);font-size:10px;letter-spacing:.12em;text-transform:uppercase;
    white-space:nowrap;
    max-width:calc(100vw - 24px);
    opacity:0;pointer-events:none;transition:opacity .35s;
    border-radius:2px;
  }
  /* Downward arrow — JS sets --arrow-x to compensate if hint is clamped */
  .roam-hint::after{
    content:'';
    position:absolute;
    top:100%;left:var(--arrow-x,50%);
    transform:translateX(-50%);
    border:5px solid transparent;
    border-top-color:rgba(26,26,28,.92);
  }
  :global(.roam-hint.on){opacity:.95}
</style>
